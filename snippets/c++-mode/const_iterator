# -*- mode: snippet -*-
# name : for (T::const_iterator it = var.begin(); it != var.end(); ++it) { ... }
# contributor : Guillaume Papin
# --
for (${1:T}::const_iterator ${2:it} = ${3:var}.begin();
    $2 != $3.end(); ++it)
  {
    $0
  }